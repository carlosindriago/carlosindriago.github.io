---
import { projects } from '../data/projects';
---

<section
  id="projects"
  class="py-section-lg bg-dark-50 bg-pattern"
>
  <div class="container mx-auto px-4">
    <!-- Section Header -->
    <div class="text-center mb-16 animate-fade-in-up">
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-4">
        Mis <span class="text-gradient">Proyectos</span>
      </h2>
      <p class="text-dark-500 text-lg max-w-2xl mx-auto">
        Aquí están algunos de los proyectos en los que he trabajado, utilizando diversas tecnologías y mejores prácticas.
      </p>
    </div>

    <!-- Projects Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-8">
      {projects.map((project, index) => (
        <article
          class="bg-dark-100 rounded-xl overflow-hidden card-hover border border-dark-200 group animate-fade-in-up"
          style={`animation-delay: ${index * 150}ms`}
        >
          <!-- Project Image -->
          <div class="relative h-48 overflow-hidden bg-dark-200">
            <img
              src={project.image}
              alt={project.title}
              class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-dark-100 to-transparent opacity-60" />
          </div>

          <!-- Project Content -->
          <div class="p-6">
            <h3 class="text-xl font-bold text-white mb-3 group-hover:text-primary-400 transition-colors">
              {project.title}
            </h3>

            <p class="text-dark-500 text-sm leading-relaxed mb-4">
              {project.description}
            </p>

            <!-- Technologies -->
            <div class="flex flex-wrap gap-2 mb-4">
              {project.technologies.map((tech) => (
                <span
                  class={`px-3 py-1 text-xs font-semibold rounded-full bg-${
                    tech.color
                  }-400/10 text-${
                    tech.color
                  }-400 border border-${tech.color}-400/20`}
                >
                  {tech.name}
                </span>
              ))}
            </div>

            <!-- Project Link -->
            {project.link && (
              <a
                href={project.link}
                class="inline-flex items-center gap-2 text-primary-400 hover:text-primary-300 text-sm font-medium transition-colors group-hover:gap-3"
              >
                Ver Proyecto
                <svg
                  class="w-4 h-4"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M17 8l4 4m0 0l-4 4m4-4H3"
                  />
                </svg>
              </a>
            )}
          </div>
        </article>
      ))}
    </div>

    <!-- Call to Action -->
    <div class="text-center mt-12">
      <a
        href="https://github.com/carlosindriago?tab=repositories"
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center gap-2 px-6 py-3 bg-primary-600 hover:bg-primary-700 text-white font-semibold rounded-lg shadow-lg hover:shadow-glow transition-all duration-300 transform hover:scale-105"
      >
        Ver Todos los Proyectos en GitHub
        <svg
          class="w-5 h-5"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"
          />
        </svg>
      </a>
    </div>
  </div>
</section>
